# Derived Figma Knowledge

Store bounded extracted text or structured notes here, not unreviewed whole-file
dumps. Each file must record the Figma source ID, file/page/frame, inspected
scope, extraction date and `candidate` or `confirmed` status.
